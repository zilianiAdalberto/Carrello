@model IEnumerable<Carrello.Models.Articolo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ArticoloNome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ArticoloNome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ArticoloId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ArticoloId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ArticoloId })
        </td>
    </tr>
}

</table>
